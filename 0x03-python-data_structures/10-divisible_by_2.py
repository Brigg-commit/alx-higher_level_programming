#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    new_l = my_list.copy()
    for i in range(0, len(my_list)):
        if my_list[i] % 2 == 0:
            list_result = divisible_by_2(list)
        else:
            list_result != divisible_by_2(list)
            return (new_l)
